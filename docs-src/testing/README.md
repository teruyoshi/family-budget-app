# テスト戦略 & カバレッジ基準

**目的**: 品質の"線引き"（合格条件）を明確化

**適用範囲**:
- 対象: 単体/統合/E2E/契約テストの定義・実行基準
- 影響: リリース判定・品質保証・開発フロー

**更新方針**:
- 変更があったらQAエンジニア・テックリードが更新
- 最低レビュー: 2名（品質観点必須）
- Freshness: 四半期毎にテスト戦略見直し

## 🏗 テスト階層定義

### 1. 単体テスト (Unit Tests)
**対象**: コンポーネント・フック・ライブラリ関数の個別動作

```bash
# 実行コマンド
make test-frontend                    # 全単体テスト実行
make test-file FILE=AmountInput.test.tsx  # 特定ファイル実行
```

**テスト範囲（軽量化方針）**:
- **UIコンポーネント**: 主要プロパティ・基本動作のみ（内部実装詳細は除外）
- **カスタムフック**: 主要状態変化・重要な副作用のみ
- **ライブラリ関数**: 基本的な入力→出力・重大エラーケースのみ
- **削除対象**: 重複テスト・過剰詳細テスト・エッジケース過多

**品質基準**:
- **カバレッジ**: Statements 60%以上、Branches 50%以上（実用的品質重視）
- **実行時間**: 1ファイル5秒以内、全体1分以内（開発効率優先）
- **安定性**: Flaky test 0%（再実行で結果変動なし）
- **テスト数**: 150テスト前後（過剰テスト削減、必要十分な品質保証）

### 2. 統合テスト (Integration Tests)
**対象**: コンポーネント間連携・ルーティング・データフロー

```bash
# 実行コマンド（単体テストと同一）
make test-frontend
```

**テスト範囲**:
- **ルーティング**: ページ遷移・URL同期・ブラウザ履歴
- **フォーム統合**: 入力→バリデーション→送信フロー
- **状態管理**: Context・カスタムフック間連携

**品質基準**:
- **シナリオカバレッジ**: 主要ユーザーフロー100%
- **クロスコンポーネント**: 境界をまたぐ処理の正確性
- **エラーハンドリング**: 異常ケースの適切な処理

### 3. E2Eテスト (End-to-End Tests)
**対象**: ユーザー視点での実際の操作フロー

```bash
# 将来実装予定
make test-e2e                         # Playwright/Cypress
```

**テスト範囲（計画・バックエンド連携時）**:
- **最優先フロー**: 支出入力（金額入力→バリデーション→保存→履歴表示）
- **次優先フロー**: 収入入力・残高計算
- **将来対応**: 認証フロー・レスポンシブ対応

### 4. 契約テスト (Contract Tests)
**対象**: Frontend↔Backend API仕様の整合性

```bash
# 将来実装予定
make contract-test                    # Pact/OpenAPI Testing
```

**テスト範囲（計画）**:
- **API仕様準拠**: リクエスト・レスポンス形式
- **エラー処理**: HTTPステータス・エラーメッセージ
- **データ型整合性**: TypeScript型↔JSON Schema

## 📊 現在の品質状況

### テスト実行状況・軽量化目標
```
⚠️ 現状: 240テスト、25スイート （実行昨2分超過で改善必要）
🎯 目標: 150テスト前後、18スイート （実行昨1分以内）
✅ 統合テスト: ルーティング・支出入力フロー （重要部分のみ）
🔄 E2Eテスト: バックエンド連携時に支出入力優先導入
❌ 契約テスト: 現在は非対応
```

### カバレッジ目標（現実的調整）
- **Statements**: 60%以上（旧目標90%から実用的に調整）
- **Branches**: 50%以上（主要条件分岐のみ）
- **Functions**: 重要関数のみテスト（内部ヘルパー関数は除外）
- **品質方針**: 動作保証 > カバレッジ数値（実際の品質を優先）

## ⚡ 効率的なテスト実行戦略

### 開発中のテスト実行
```bash
# 1. 変更ファイル関連テストのみ（推奨）
make test-file FILE=修正したコンポーネント.test.tsx

# 2. 特定機能グループのテスト
make test-file FILE="Money"      # 金額関連
make test-file FILE="Navigation" # ナビゲーション関連
make test-file FILE="Form"       # フォーム関連
```

### コミット前・プル前のテスト
```bash
# 全テスト実行（必須）
make test-frontend

# 品質チェック統合実行
make quality-check-frontend
```

### CI/CD でのテスト実行
```bash
# GitHub Actions で自動実行
- テスト実行（並列化）
- カバレッジレポート生成
- 品質ゲート評価
```

## 🛠 テスト最適化設定

### Jest設定最適化（済み）
```javascript
// jest.config.js の最適化内容
{
  testTimeout: 15000,           // タイムアウト延長
  setupFilesAfterEnv: ['<rootDir>/src/setupTests.ts'],
  testEnvironment: 'jsdom',
  transform: { /* TypeScript対応 */ },
  moduleNameMapping: { /* パスエイリアス */ }
}
```

### パフォーマンス改善（済み）
- **act()警告解決**: React状態更新の適切なラップ
- **Ripple無効化**: MUIアニメーション無効化で高速化
- **メモリ管理**: テスト間でのクリーンアップ

### テストユーティリティ
```typescript
// src/__tests__/test-utils/ 
- routing.tsx           # ルーティングテスト用ラッパー
- optimization.setup.ts # パフォーマンス最適化設定
```

## 📋 品質ゲート基準

### リリース前必須チェック（軽量化対応）
- [ ] **全テスト通過**: 150テスト前後、0エラー、0重大警告
- [ ] **カバレッジ基準**: Statements 60%以上（動作保証があること）
- [ ] **パフォーマンス**: 全テスト1分以内完了（開発効率優先）
- [ ] **安定性**: 3回連続実行で同一結果
- [ ] **主要機能**: 支出入力フロー動作確認

### コード品質統合チェック
```bash
make quality-check-frontend
```
1. **Prettier**: コードフォーマット統一
2. **ESLint**: コード品質・ベストプラクティス
3. **TypeScript**: 型チェック・strict mode
4. **Jest**: テスト実行・カバレッジ
5. **Vite Build**: プロダクションビルド確認

## 🔍 テストデバッグ・トラブルシューティング

### よくある問題と解決策

#### 1. テスト実行が遅い（最優先対応）
```bash
# 特定ファイルのみ実行
make test-file FILE=遅いテスト.test.tsx

# 重複テストの特定・削除を2024年12月優先実施
# 過剰詳細テストの削除で実行昨1分以内を目指す

# キャッシュクリア
cd frontend && npm test -- --clearCache
```

#### 2. act() 警告が発生
```typescript
// 解決済みパターン（既存コード参照）
await act(async () => {
  fireEvent.click(button);
  await waitFor(() => expect(/* 条件 */).toBeTruthy());
});
```

#### 3. タイムアウトエラー
```bash
# Jest設定で15秒に延長済み
# さらに延長が必要な場合は個別にtimeout設定
test('heavy test', async () => {
  // テストコード
}, 30000); // 30秒
```

#### 4. モックが効かない
```typescript
// 適切なモック設定例
jest.mock('@/hooks/useMoney', () => ({
  useMoney: jest.fn()
}));
```

## 🚀 テスト戦略の進化

### 短期計画（次四半期・優先度順）
1. **テスト軽量化**: 重複テスト削除で実行昨1分以内を実現（最優先）
2. **E2Eテスト準備**: バックエンド連携時に支出入力フローのPlaywright導入
3. **品質基準調整**: カバレッジ60%目標への実運用調整
4. **非対応**: ビジュアル回帰テスト・契約テスト

### 中期計画（半年〜1年・選択的導入）
1. **パフォーマンステスト**: 重要コンポーネントのみ部分的導入検討
2. **非対応**: アクセシビリティテスト
3. **非対応**: セキュリティテスト

### 品質文化醸成（実用的アプローチ）
- **効率重視**: 開発速度と品質のバランスを重視したテスト戦略
- **動作保証優先**: カバレッジ数値より実際の動作品質を優先
- **適切な範囲**: 必要十分なテストのみで品質を保証、過剰テストを回避
- **定期見直し**: 四半期毎にテスト効率と品質のバランスを再評価